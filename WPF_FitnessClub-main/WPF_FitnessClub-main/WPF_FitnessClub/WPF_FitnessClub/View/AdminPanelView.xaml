<UserControl x:Class="WPF_FitnessClub.View.AdminPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_FitnessClub.View"
             xmlns:viewmodels="clr-namespace:WPF_FitnessClub.ViewModels"
             xmlns:converters="clr-namespace:WPF_FitnessClub.Converters"
             xmlns:models="clr-namespace:WPF_FitnessClub.Models"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/WindowStyles.xaml"/>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
                <ResourceDictionary Source="/Styles/TextStyles.xaml"/>
                <ResourceDictionary Source="/Styles/DataGridStyles.xaml" />
                <ResourceDictionary Source="/Styles/TabControlStyles.xaml" />
                <ResourceDictionary Source="/Styles/ComboBoxStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:RoleToStringConverter x:Key="RoleToStringConverter"/>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            
            <!-- Доступные роли пользователя для ComboBox -->
            <ObjectDataProvider x:Key="UserRoleValues" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="models:UserRole"/>
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="{DynamicResource AdminPanelTitle}" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="10" 
                   HorizontalAlignment="Center" 
                   Style="{StaticResource HeaderTextBlockStyle}"/>
        
        <!-- TabControl для таблиц базы данных -->
        <TabControl Grid.Row="1" 
            Margin="5" 
            Style="{StaticResource CustomTabControlStyle}">
            <!-- Вкладка пользователей -->
            <TabItem Header="{DynamicResource AdminPanelUsersTab}" 
             Style="{StaticResource CustomTabItemStyle}">
                <Grid>
                    <!-- DataGrid для отображения пользователей -->
                    <DataGrid x:Name="UsersDataGrid" 
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      AlternatingRowBackground="AliceBlue"
                      BorderBrush="Gray"
                      BorderThickness="1"
                      GridLinesVisibility="All"
                      HorizontalGridLinesBrush="LightGray"
                      VerticalGridLinesBrush="LightGray"
                      Margin="10"
                      ItemsSource="{Binding UsersTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                      CellEditEnding="UsersDataGrid_CellEditEnding"
                      RowHeight="38" 
                      Style="{StaticResource DefaultDataGridStyle}">

                        <!-- РЕСУРСЫ ТАБЛИЦЫ (ЧИНЯТ ВЫЛЕТ И ОКРУГЛЕНИЯ) -->
                        <DataGrid.Resources>
                            <!-- 1. Стиль для ввода текста (оранжевое/зеленое подчеркивание без синевы) -->
                            <Style x:Key="ColumnFocusStyle" TargetType="TextBox">
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryRedBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="Transparent" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            CornerRadius="0">
                                                <ScrollViewer x:Name="PART_ContentHost"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <!-- 2. Стиль ячейки (убирает системное синее скругление) -->
                            <Style TargetType="DataGridCell">
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <!-- CornerRadius="0" делает углы острыми -->
                                            <Border Background="{TemplateBinding Background}" CornerRadius="0">
                                                <ContentPresenter VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource LightGrayBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource AdminPanelIdColumn}" Binding="{Binding Id}" IsReadOnly="True" Width="40"/>

                            <DataGridTextColumn Header="{DynamicResource FullName}" Binding="{Binding FullName}" Width="*" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource Email}" Binding="{Binding Email}" Width="*" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource Login}"  Binding="{Binding Login}" Width="*" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>

                            <DataGridTemplateColumn Header="{DynamicResource AdminPanelRoleColumn}" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Role, Converter={StaticResource RoleToStringConverter}}" VerticalAlignment="Center" Margin="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                          SelectedValuePath="Tag" VerticalContentAlignment="Center" Height="30">
                                            <ComboBoxItem Content="Клиент" Tag="{x:Static models:UserRole.Client}"/>
                                            <ComboBoxItem Content="Тренер" Tag="{x:Static models:UserRole.Coach}"/>
                                            <ComboBoxItem Content="Админ" Tag="{x:Static models:UserRole.Admin}"/>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="{DynamicResource PhoneLabel}" Binding="{Binding Phone}" Width="120" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource WeightLabel}" Binding="{Binding Weight}" Width="60" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource HeightLabel}" Binding="{Binding Height}" Width="60" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource AgeLabel}" Binding="{Binding Age}" Width="60" EditingElementStyle="{StaticResource ColumnFocusStyle}"/>

                            <DataGridCheckBoxColumn Header="{DynamicResource AdminPanelBlockedColumn}" Binding="{Binding IsBlocked, UpdateSourceTrigger=PropertyChanged}" Width="60"/>

                            <DataGridTemplateColumn Header="{DynamicResource AdminPanelActionsColumn}" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <!-- КНОПКА: Без контура, острые углы, текст влазит -->
                                        <Button Content="{DynamicResource DeleteButton}" 
                                        Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"
                                        Background="{DynamicResource PrimaryRedBrush}" 
                                        Foreground="White"
                                        BorderThickness="0" 
                                        FontSize="11" 
                                        Padding="0" 
                                        Margin="5,4"
                                        Height="24" 
                                        Width="85"
                                        HorizontalAlignment="Center">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="0"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="{DynamicResource AdminPanelRefreshButton}" 
                    Margin="5"
                    Padding="10,5"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource DefaultButtonStyle}"/>
            <Button Content="{DynamicResource AdminPanelAddUserButton}" 
                    Margin="5"
                    Padding="10,5"
                    Command="{Binding AddUserCommand}"
                    Style="{StaticResource DefaultButtonStyle}"/>
        </StackPanel>
        
    </Grid>
</UserControl> 