<UserControl x:Class="WPF_FitnessClub.View.AdminPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_FitnessClub.View"
             xmlns:viewmodels="clr-namespace:WPF_FitnessClub.ViewModels"
             xmlns:converters="clr-namespace:WPF_FitnessClub.Converters"
             xmlns:models="clr-namespace:WPF_FitnessClub.Models"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/WindowStyles.xaml"/>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
                <ResourceDictionary Source="/Styles/TextStyles.xaml"/>
                <ResourceDictionary Source="/Styles/DataGridStyles.xaml" />
                <ResourceDictionary Source="/Styles/TabControlStyles.xaml" />
                <ResourceDictionary Source="/Styles/ComboBoxStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:RoleToStringConverter x:Key="RoleToStringConverter"/>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            
            <!-- Доступные роли пользователя для ComboBox -->
            <ObjectDataProvider x:Key="UserRoleValues" MethodName="GetValues" ObjectType="{x:Type system:Enum}">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="models:UserRole"/>
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="{DynamicResource AdminPanelTitle}" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="10" 
                   HorizontalAlignment="Center" 
                   Style="{StaticResource HeaderTextBlockStyle}"/>
        
        <!-- TabControl для таблиц базы данных -->
        <TabControl Grid.Row="1" 
                    Margin="5" 
                    Style="{StaticResource DefaultTabControlStyle}">
            <!-- Вкладка пользователей -->
            <TabItem Header="{DynamicResource AdminPanelUsersTab}" 
                     Style="{StaticResource DefaultTabItemStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- DataGrid для отображения пользователей -->
                    <DataGrid x:Name="UsersDataGrid" 
                              AutoGenerateColumns="False"
                              IsReadOnly="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="True"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              AlternatingRowBackground="AliceBlue"
                              BorderBrush="Gray"
                              BorderThickness="1"
                              GridLinesVisibility="All"
                              HorizontalGridLinesBrush="LightGray"
                              VerticalGridLinesBrush="LightGray"
                              Margin="10"
                              ItemsSource="{Binding UsersTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                              CellEditEnding="UsersDataGrid_CellEditEnding"
                              Style="{StaticResource DefaultDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="40"/>
                            <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="*"/>

                            <DataGridTemplateColumn Header="Роль" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Role, Converter={StaticResource RoleToStringConverter}}" VerticalAlignment="Center" Margin="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <!-- Используем Tag="1", "2", "3" для связи с C#-кодом -->
                                        <ComboBox SelectedValue="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag">
                                            <ComboBoxItem Content="Клиент" Tag="{x:Static models:UserRole.Client}"/>
                                            <ComboBoxItem Content="Тренер" Tag="{x:Static models:UserRole.Coach}"/>
                                            <ComboBoxItem Content="Админ" Tag="{x:Static models:UserRole.Admin}"/>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                            <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="60"/>
                            <DataGridTextColumn Header="Рост" Binding="{Binding Height}" Width="60"/>
                            <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="60"/>

                            <DataGridCheckBoxColumn Header="Забл." Binding="{Binding IsBlocked, UpdateSourceTrigger=PropertyChanged}" Width="60"/>

                            <DataGridTemplateColumn Header="Действия" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Удалить" 
                            Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                            CommandParameter="{Binding}"
                            Background="#E11A22" 
                            Foreground="White"
                            FontSize="10" 
                            Padding="2" 
                            Margin="2"
                            Height="22" 
                            Width="70"
                            Style="{StaticResource DefaultButtonStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        
        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="{DynamicResource AdminPanelRefreshButton}" 
                    Margin="5"
                    Padding="10,5"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource DefaultButtonStyle}"/>
            <Button Content="{DynamicResource AdminPanelAddUserButton}" 
                    Margin="5"
                    Padding="10,5"
                    Command="{Binding AddUserCommand}"
                    Style="{StaticResource DefaultButtonStyle}"/>
        </StackPanel>
        
    </Grid>
</UserControl> 