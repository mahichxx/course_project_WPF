<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Основной стиль TabControl -->
    <Style x:Key="CustomTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- Центрируем вкладки и убираем отступы -->
                        <Border Grid.Row="0" BorderThickness="0" Background="Transparent" HorizontalAlignment="Center">
                            <TabPanel IsItemsHost="True"/>
                        </Border>
                        <Border Grid.Row="1" Background="{TemplateBinding Background}">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для TabItem (Слова Авторизация/Регистрация) -->
    <Style x:Key="CustomTabItemStyle" TargetType="TabItem">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <!-- CornerRadius="0" делает углы абсолютно прямыми -->
                    <Border x:Name="Border" 
                            Padding="{TemplateBinding Padding}" 
                            BorderThickness="0,0,0,2" 
                            BorderBrush="Transparent" 
                            Background="Transparent" 
                            CornerRadius="0">
                        <ContentPresenter x:Name="ContentSite" ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Активная вкладка -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                        </Trigger>
                        <!-- Наведение мышкой -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#80FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Псевдонимы для совместимости с другими окнами -->
    <Style x:Key="DefaultTabControlStyle" BasedOn="{StaticResource CustomTabControlStyle}" TargetType="TabControl"/>
    <Style x:Key="DefaultTabItemStyle" BasedOn="{StaticResource CustomTabItemStyle}" TargetType="TabItem"/>

</ResourceDictionary>