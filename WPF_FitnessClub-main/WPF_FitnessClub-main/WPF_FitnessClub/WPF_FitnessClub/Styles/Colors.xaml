<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WPF_FitnessClub">

    <Color x:Key="PrimaryRed">#FF7A50</Color>
    <!-- Коралловый акцент -->
    <Color x:Key="DarkRed">#E6643C</Color>
    <!-- Темно-оранжевый для наведения -->
    <Color x:Key="BackgroundColor">#efeded</Color>
    <!-- Светло-серый фон -->
    <Color x:Key="SecondaryBackgroundColor">#fdf3f0</Color>
    <!-- Цвет карточек -->
    <Color x:Key="TextColor">#252727</Color>
    <!-- Почти черный текст -->
    <Color x:Key="MediumGray">#808080</Color>
    <Color x:Key="LightGray">#D1D9D6</Color>

    <!-- DynamicResource -->
    <SolidColorBrush x:Key="PrimaryRedBrush" Color="{DynamicResource PrimaryRed}"/>
    <SolidColorBrush x:Key="DarkRedBrush" Color="{DynamicResource DarkRed}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{DynamicResource TextColor}"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="{DynamicResource SecondaryBackgroundColor}"/>
    <SolidColorBrush x:Key="LightGrayBrush" Color="{DynamicResource LightGray}"/>
    <SolidColorBrush x:Key="MediumGrayBrush" Color="{DynamicResource MediumGray}"/>

    <!-- Текстовые поля -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Padding" Value="2,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource LightGrayBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontSize" Value="14"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryRedBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Кнопка входа (Главная кнопка) -->
    <Style x:Key="EnterButton" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Padding" Value="20,5"/>
        <Setter Property="Background" Value="{DynamicResource DarkRedBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="White"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimaryRedBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Выпадающие списки -->
    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Height" Value="35"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource LightGrayBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Полоса прокрутки -->
    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- Рамки (Карточки) -->
    <Style TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
        <Setter Property="CornerRadius" Value="10"/>
    </Style>

</ResourceDictionary>